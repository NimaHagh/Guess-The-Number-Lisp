This Lisp program will guess the users selected number using binary search.

The program will use global variables to store the lower and upper bound numbers.

Since the values of *lowerb* and *upperb* are changing as the user plays the game,-
a function has been defined that resets the game parameters and enables us to start over.

ASH function to half a number by shiftting its bits one position to-
the right.This one bit shift operation is executed much faster than a division by two using the ’/’ operator.

